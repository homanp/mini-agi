# Required
TELEGRAM_BOT_TOKEN=your_telegram_bot_token
ANTHROPIC_API_KEY=your_anthropic_api_key

# Web Search (optional - Parallel Web)
PARALLEL_API_KEY=your_parallel_api_key

# LLM (defaults shown)
LLM_PROVIDER=anthropic
LLM_MODEL=claude-sonnet-4-20250514

# Workspace - where bash commands run (default: current directory)
WORKSPACE_ROOT=/path/to/workspace

# Memory - conversation logs as markdown (default: WORKSPACE_ROOT/memory)
MEMORY_ENABLED=true
MEMORY_DIR=/path/to/memory

# Active task context in prompt (optional)
ACTIVE_TASKS_CONTEXT_MAX_CHARS=3000
ACTIVE_TASKS_CONTEXT_MAX_ITEMS=8

# Browser profile preference for agent-browser CDP attach
AGENT_BROWSER_CHROME_PROFILE_NAME=picobot
AGENT_BROWSER_AUTO_BOOTSTRAP_PROFILE=true

# Session - conversation transcripts as JSONL (default: WORKSPACE_ROOT/session)
SESSION_DIR=/path/to/session

# Optional: restrict bot access to specific users
TELEGRAM_ALLOWED_USERS=
